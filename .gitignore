__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
node_modules/
.env
_commitmsg.txt

# Dữ liệu người dùng (chứa mật khẩu hash + token phiên) — KHÔNG đẩy lên
data/users.json

# File train trung gian (tái tạo được bằng scripts/) — bỏ để repo gọn
data/train_clean.csv
data/train_combined.csv

# Thư mục model dư thừa (models/ là bản chính đang dùng)
models_old/
models_v2/
data/learned_context.jsonl

# US15/US18: log lịch sử dự đoán + feedback (runtime, sinh khi chạy) — KHÔNG đẩy lên
data/prediction_log.jsonl
data/feedback.jsonl

# Ảnh chụp UI/test (nặng, tái tạo được khi chạy lại test) — không track để repo gọn.
# Vẫn giữ trên máy local; ai cần xem chạy lại script test tương ứng.
# Riêng báo cáo .md/.txt trong screenshots/ VẪN track làm hồ sơ QA.
screenshots/*
!screenshots/*.md
!screenshots/*.txt
