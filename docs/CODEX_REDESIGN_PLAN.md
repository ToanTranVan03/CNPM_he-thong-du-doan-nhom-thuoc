# Kế hoạch redesign toàn bộ giao diện PharmaPredict

> Phạm vi: lập kế hoạch triển khai cho frontend HTML/CSS/JS thuần, giữ nguyên hành vi hiện có và bám theo `docs/CODEX_REDESIGN_BRIEF.md`, `docs/DESIGN_SYSTEM.md`, `docs/FIGMA_BUILD_CHECKLIST.md`.
>
> Thứ tự ưu tiên: **Giai đoạn 0 nền tảng -> Giai đoạn 1 component -> trang Kết quả -> các màn còn lại -> responsive/trạng thái -> QA**.

## Giai đoạn -1 — Chuẩn bị và chiến lược bàn giao (Claude bổ sung khi review)

- [ ] **-1.1 Tạo nhánh feature riêng** — **File:** Repo. **Mô tả:** Tách nhánh `feat/redesign-ui` từ `develop`; KHÔNG sửa trực tiếp `develop`. Refactor lớn phải cô lập để dễ rollback. **RỦI RO JS:** Không.
- [ ] **-1.2 Bàn giao theo MỐC, không làm một lần 40+ mục** — **File:** Không áp dụng. **Mô tả:** Chia 2 mốc để giảm rủi ro vỡ contract khó truy vết:
  - **Mốc 1 (MVP):** Giai đoạn 0 (tokens + dark mode) + Giai đoạn 1 (component) + trang Kết quả (2A). → Antigravity verify → review giữa kỳ.
  - **Mốc 2:** Các trang còn lại (Auth, Trang chủ, Lịch sử, Hồ sơ) + Giai đoạn 3 (responsive/trạng thái) + Giai đoạn 4 (QA tổng).
  Chỉ sang Mốc 2 khi Mốc 1 pass Antigravity và không vỡ DOM contract. **RỦI RO JS:** Không.

## Nguyên tắc và contract phải khóa trước khi triển khai

- [ ] **Khóa phạm vi công nghệ** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Chỉ dùng HTML/CSS/JS thuần; không thêm framework, package, bundler, build step hoặc dependency runtime mới. **RỦI RO JS:** Không.
- [ ] **Lập snapshot DOM contract trước khi sửa** — **File:** `frontend/index.html`, `frontend/script.js`. **Mô tả:** Chụp lại danh sách ID, selector, class trạng thái và `data-*` hiện tại; dùng làm checklist đối chiếu sau từng giai đoạn. **RỦI RO JS:** **CÓ - cao**, vì đổi/xóa contract sẽ làm listener hoặc render động bị lỗi.
- [ ] **Giữ nguyên 53 ID được `script.js` truy cập trực tiếp** — **File:** `frontend/index.html`. **Mô tả:** Không đổi tên, xóa hoặc tạo trùng các ID sau:
  `auth-screen`, `care-list`, `case-description`, `char-count`, `clear-case`, `confidence-bar`, `confidence-value`, `diagnosis-form`, `example-case`, `forgot-email`, `forgot-form`, `forgot-message`, `form-message`, `history-list`, `history-search`, `login-email`, `login-form`, `login-message`, `login-password`, `logout-button`, `medication-list`, `precaution-list`, `profile-logout-button`, `profile-summary`, `register-email`, `register-form`, `register-message`, `register-name`, `register-password`, `reset-code`, `reset-email`, `reset-form`, `reset-message`, `reset-password`, `result-note`, `result-subtitle`, `result-title`, `save-result`, `score-label`, `selected-count`, `suggested-symptoms-card`, `suggested-symptoms-list`, `summary-diagnosis`, `summary-drug-group`, `summary-medication-name`, `symptom-list`, `symptom-search`, `top-predictions`, `top-predictions-title`, `user-avatar`, `user-email`, `user-name`, `warning-text`. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **Giữ nguyên contract điều hướng và auth** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Bảo toàn `.app-shell`, `.page`, `.is-active`, `.is-hidden`, `[data-page]`, `[data-auth-view]`, `[data-auth-target]`; giữ các trang `page-home`, `page-result`, `page-history`, `page-about` và giá trị `data-page="home|result|history|about"` để tiếp tục khớp quy ước `page-${pageName}`. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **Giữ contract nội dung sinh động** — **File:** `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Bảo toàn hoặc cập nhật đồng bộ các class/thuộc tính do JS tạo và truy vấn: `.history-card`, `.user-history-card`, `.symptom-chip`, `.suggested-symptom-chip`, `.status-pill`, `.status-success`, `data-search`, `data-symptom`, `.is-selected`, `.is-error`. **LƯU Ý (Claude review):** `data-symptom` được set qua `button.dataset.symptom = symptom.id` (dòng 230, 279) — write-only, không bị query lại; trạng thái chọn theo dõi qua JS Set `selectedSymptoms`. Giữ thuộc tính là an toàn. **RỦI RO JS:** **CÓ - cao**.
- [ ] **Quy ước nhãn rủi ro trong plan** — **File:** Không áp dụng. **Mô tả:** `Không` = CSS/nội dung tĩnh; `Thấp` = thêm markup không đổi contract; `Cao` = chỉnh DOM do JS render hoặc listener; `Nghiêm trọng` = đụng ID, selector điều hướng/auth hoặc luồng dự đoán. **RỦI RO JS:** Không.

## Giai đoạn 0 — Tokens và nền tảng

- [ ] **0.1 Khai báo đầy đủ color tokens Light** — **File:** `frontend/styles.css`. **Mô tả:** Thay hệ token cũ bằng `--primary`, `--primary-hover`, `--primary-soft`, `--primary-contrast`, `--accent`, `--accent-soft`, semantic colors, `--bg`, `--surface`, `--surface-muted`, `--border`, `--text`, `--text-muted`, `--text-subtle` đúng `DESIGN_SYSTEM.md`; loại bỏ màu hardcode khỏi component khi đã có token tương ứng. **RỦI RO JS:** Không.
- [ ] **0.2 Khai báo token Dark mode** — **File:** `frontend/styles.css`. **Mô tả:** Tạo `[data-theme="dark"]` với toàn bộ token đối ứng; bổ sung giá trị dark hợp lý cho token tài liệu chưa nêu rõ, đặc biệt soft colors, accent, semantic, elevation và placeholder; kiểm tra tương phản chữ tối thiểu 4.5:1. **RỦI RO JS:** Không.
- [ ] **0.3 Chuẩn hóa typography tokens** — **File:** `frontend/styles.css`. **Mô tả:** Khai báo type scale Inter từ display đến xs, line-height, font-weight, letter-spacing; bật `font-variant-numeric: tabular-nums` cho số liệu, phần trăm và ngày. **RỦI RO JS:** Không.
- [ ] **0.4 Chuẩn hóa spacing, radius, shadow và layout tokens** — **File:** `frontend/styles.css`. **Mô tả:** Khai báo `--space-1` đến `--space-8`, `--radius-sm|md|lg|pill`, `--shadow-sm|md|lg`, chiều rộng sidebar 240/72px, content max-width 1200px và breakpoint `<640`, `640-1024`, `>1024`. **RỦI RO JS:** Không.
- [ ] **0.5 Refactor base/reset và surface hierarchy** — **File:** `frontend/styles.css`. **Mô tả:** Chuẩn hóa body, heading, paragraph, link, button, input, textarea, table, list, selection, scrollbar; thay nền/viền/chữ hardcode bằng token và giảm card/shadow không cần thiết để giao diện y tế gọn, rõ. **RỦI RO JS:** Không.
- [ ] **0.6 Thêm focus system dùng chung** — **File:** `frontend/styles.css`. **Mô tả:** Dùng `:focus-visible` với ring 2px màu primary và offset rõ trên light/dark; không xóa outline nếu chưa có thay thế; bảo đảm trạng thái hover không làm layout dịch chuyển. **RỦI RO JS:** Không.
- [ ] **0.7 Thêm dark mode toggle toàn cục** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Thêm một icon-button có `aria-label`, trạng thái `aria-pressed`, target tối thiểu 44x44px và dùng được ở auth lẫn app; đặt/xóa `data-theme="dark"` trên `<html>`. Không tái sử dụng hoặc đổi tên ID hiện hữu; ID mới phải duy nhất. **RỦI RO JS:** **CÓ - cao**, vì thêm listener và thay đổi thuộc tính root.
- [ ] **0.8 Lưu và khởi tạo theme** — **File:** `frontend/index.html`, `frontend/script.js`. **Mô tả:** Lưu lựa chọn vào `localStorage` với key riêng; lần đầu theo `prefers-color-scheme`, các lần sau ưu tiên giá trị đã lưu; khởi tạo theme sớm để hạn chế flash sai theme và cập nhật icon/nhãn hỗ trợ đọc màn hình. **RỦI RO JS:** **CÓ - cao**, cần tránh ảnh hưởng các key auth/history hiện hữu.
- [ ] **0.9 Nghiệm thu nền tảng Light/Dark** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Antigravity verify auth và bốn trang ở cả hai theme; kiểm tra reload vẫn giữ theme, không flash rõ, không mất nội dung, không phát sinh lỗi console. **RỦI RO JS:** **CÓ - thấp**.

## Giai đoạn 1 — Component CSS dùng lại

- [ ] **1.1 Button variants** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuẩn hóa primary/secondary/ghost/danger, md 44px, sm 36px, icon/no-icon, hover/focus/disabled/loading; ánh xạ class cũ như `.primary-button`, `.secondary-button`, `.text-button`, `.icon-button` sang cùng hệ component để giảm thay đổi markup. **RỦI RO JS:** Thấp.
- [ ] **1.2 Input, textarea và field message** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuẩn hóa label, placeholder, leading icon, default/focus/error/disabled, helper text; giữ nguyên liên kết `label[for]`, ID input và vùng message `role="status"`. **RỦI RO JS:** **CÓ - cao** nếu bọc/di chuyển input làm sai ID hoặc form.
- [ ] **1.3 Chip triệu chứng** — **File:** `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Thiết kế default/selected/disabled theo token; selected có icon/check hoặc nhãn hỗ trợ, không chỉ đổi màu; bảo đảm chip sinh động có target 44px trên mobile và trạng thái bàn phím rõ. **RỦI RO JS:** **CÓ - cao**, vì chip được tạo trong `renderSymptoms()` và `renderSuggestedSymptoms()`.
- [ ] **1.4 Status pill** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Chuẩn hóa success/warning/danger/neutral/secure với icon + nhãn chữ; giữ các class trạng thái JS đang tạo cho history card và thêm semantic variant khi cần. **RỦI RO JS:** **CÓ - cao** với card lịch sử sinh động.
- [ ] **1.5 Card và panel variants** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuẩn hóa default/featured/warning/dark; chỉ dùng card khi nhóm nội dung hoặc tương tác cần ranh giới, giảm dashboard-card mosaic và shadow dày; bảo toàn các container có ID. **RỦI RO JS:** Thấp.
- [ ] **1.6 Nav item desktop/mobile** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Đồng bộ sidebar/bottom nav, trạng thái active, icon, nhãn, focus và target 44px; giữ nguyên `[data-page]`, `.is-active`, thứ tự và giá trị trang. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **1.7 Avatar và user summary** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuẩn hóa avatar sm/md, truncation tên/email và nút logout; giữ nguyên `user-avatar`, `user-name`, `user-email`, `logout-button`. **RỦI RO JS:** **CÓ - cao**.
- [ ] **1.8 Confidence Gauge** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Chuyển vùng độ tin cậy thành gauge/donut hoặc gauge trực quan tương đương, giữ `confidence-value`, `confidence-bar`, `score-label`; hiển thị đồng thời phần trăm/trạng thái chữ `Thấp`, `Trung bình`, `Cao` hoặc `Chưa đủ`; ánh xạ `<50`, `50-74`, `>=75` sang danger/warning/success. **RỦI RO JS:** **CÓ - cao**, vì `renderPrediction()` và `renderInsufficientInput()` đang đặt text/width trực tiếp.
- [ ] **1.9 Prediction Bar top-3** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Render mỗi dự đoán với tên, phần trăm và thanh tỷ lệ; giữ nguyên `top-predictions`, `top-predictions-title`; hỗ trợ trường hợp rule và danh sách rỗng. **RỦI RO JS:** **CÓ - cao**, vì thay cấu trúc DOM do `renderPrediction()` sinh ra.
- [ ] **1.10 Skeleton, empty state và inline alert** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Tạo component dùng lại cho loading/empty/error/offline/không đủ tin cậy; dùng `aria-live` phù hợp, icon + tiêu đề + mô tả + hành động, không truyền trạng thái chỉ bằng màu. **RỦI RO JS:** **CÓ - cao**, vì cần nối vào fetch/auth/predict/render hiện có.
- [ ] **1.11 Nghiệm thu component** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Antigravity verify variants ở Light/Dark, keyboard-only, zoom 200%, target mobile và các component do JS tạo sau tương tác. **RỦI RO JS:** **CÓ - thấp**.

## Giai đoạn 2 — Redesign từng trang

### 2A. Trang Kết quả — ưu tiên cao nhất

- [ ] **2A.1 Tái cấu trúc layout kết quả desktop** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Dùng grid trái = summary + gauge sticky, phải = chi tiết; thứ tự đọc trong 3 giây phải là kết luận dự kiến -> độ tin cậy -> nhóm thuốc -> cảnh báo/disclaimer. Giữ nguyên toàn bộ ID kết quả. **RỦI RO JS:** **CÓ - cao** do di chuyển các node JS cập nhật.
- [ ] **2A.2 Thiết kế summary theo trạng thái an toàn** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Tạo presentation rõ cho empty, đủ tin cậy, trung bình, thấp/không đủ dữ liệu và rule-based; thêm nhãn chữ, icon, thông điệp hành động; vô hiệu hóa hoặc ẩn hợp lý nút lưu khi `currentResult` không hợp lệ nhưng không thay đổi luồng điều hướng ngoài chủ đích. **RỦI RO JS:** **CÓ - nghiêm trọng**, vì đụng `renderPrediction()`, `renderInsufficientInput()` và `save-result`.
- [ ] **2A.3 Tích hợp confidence scale** — **File:** `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Gán class/data-state theo ngưỡng `<50`, `50-74`, `>=75`; cập nhật gauge, status pill và copy đồng bộ; trường hợp rule hiển thị nhãn riêng thay vì giả lập phần trăm. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2A.4 Làm rõ tóm tắt bệnh án** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Giữ `summary-diagnosis`, `summary-medication-name`, `summary-drug-group`; desktop có thể dùng bảng, mobile chuyển thành danh sách nhãn-giá trị dễ đọc mà không duplicate ID. **RỦI RO JS:** **CÓ - cao** nếu thay cell chứa ID.
- [ ] **2A.5 Chuẩn hóa khối xử trí, phòng ngừa, chăm sóc** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Tạo hierarchy và icon định hướng cho `medication-list`, `precaution-list`, `care-list`; giữ disclaimer/cảnh báo luôn hiện diện và đặt `warning-text` ở vùng dễ thấy. **RỦI RO JS:** **CÓ - cao** do các danh sách được render lại bằng JS.
- [ ] **2A.6 Redesign gợi ý bổ sung triệu chứng** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Giữ `suggested-symptoms-card`, `suggested-symptoms-list`, `.is-hidden`; giải thích vì sao cần bổ sung và trạng thái đang chạy dự đoán lại sau khi chọn chip. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2A.7 Redesign top predictions** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Thay minh họa phân tử trang trí bằng Prediction Bar có giá trị quyết định; hiển thị tối đa top-3, tabular nums, label nguồn score và fallback rõ ràng. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2A.8 Hoàn thiện hành động kết quả** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Phân cấp `Lưu kết quả` và `Dự đoán mới`, thêm disabled/loading/saved feedback; giữ `save-result` và `data-page="home"`. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **2A.9 Antigravity verify trang Kết quả** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Kiểm tra đủ các fixture: chưa có kết quả, rule, low, mid, high, needs_more_input, API error, top predictions rỗng/dài; đối chiếu Figma Desktop/Mobile và không có lỗi console. **RỦI RO JS:** **CÓ - cao**.

### 2B. Auth

- [ ] **2B.1 Redesign shell auth** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Tạo bố cục tin cậy y khoa, copy ngắn, một visual anchor nhẹ và form là trọng tâm; hỗ trợ mobile một cột; dark toggle luôn truy cập được. **RỦI RO JS:** Thấp.
- [ ] **2B.2 Chuẩn hóa bốn auth view** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Đồng bộ login/register/forgot/reset, giữ nguyên `[data-auth-view]`, `[data-auth-target]`, các form/input/message ID và `.is-hidden`; hiển thị error/status không làm layout nhảy mạnh. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **2B.3 Kiểm tra autocomplete và a11y auth** — **File:** `frontend/index.html`. **Mô tả:** Giữ autocomplete phù hợp, label thật, required/minlength, `role="status"`; thêm mô tả lỗi được liên kết bằng `aria-describedby` nếu cần. **RỦI RO JS:** **CÓ - cao** nếu thay cấu trúc form/input.

### 2C. Trang Chủ

- [ ] **2C.1 Tái cấu trúc workspace nhập bệnh án** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Desktop hai cột: form chiếm chính, side-stack metric/tip; tablet xếp dọc; rút gọn phần giới thiệu để người dùng tiếp cận form nhanh. Giữ `diagnosis-form` và mọi ID đầu vào. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2C.2 Redesign textarea và bộ đếm** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Làm rõ label, ví dụ, giới hạn ký tự, trạng thái focus/error; giữ `case-description`, `char-count`, maxlength 2000. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2C.3 Redesign symptom picker** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Giữ `symptom-search`, `symptom-list`, `selected-count`; bổ sung empty/loading/error rõ, chip bàn phím được, vùng cuộn có affordance và số lượng chọn không chỉ thể hiện bằng màu. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2C.4 Chuẩn hóa CTA và helper actions** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Phân cấp submit, `clear-case`, `example-case`; thêm confirmation/disabled hợp lý nếu cần nhưng không đổi event target; giữ `form-message` là vùng thông báo. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2C.5 Redesign hoạt động gần đây** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuyển card tĩnh thành danh sách/summary gọn, ưu tiên thông tin quét nhanh và CTA `[data-page="history"]`; tránh cạnh tranh với form chính. **RỦI RO JS:** **CÓ - cao** ở nút điều hướng.

### 2D. Lịch sử

- [ ] **2D.1 Redesign header, search và action** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Làm rõ search là chức năng chính; nút lọc/xuất chỉ giữ khi có hành vi hoặc thể hiện disabled/coming-soon đúng a11y, tránh affordance giả. Giữ `history-search`. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2D.2 Redesign history list/card** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Đồng bộ card tĩnh và card do `createHistoryCard()` tạo; giữ `.history-card`, `.user-history-card`, `data-search`, status/date/title/summary/action; responsive một đến ba cột theo nội dung. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2D.3 Empty và no-search-result** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Hiển thị empty state khi chưa có lịch sử và trạng thái không khớp tìm kiếm; không phá cơ chế `.is-hidden` hiện tại. **RỦI RO JS:** **CÓ - cao**.

### 2E. Hồ sơ

- [ ] **2E.1 Redesign account summary** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Đặt thông tin tài khoản và hành động đăng xuất rõ ràng; giữ `profile-summary`, `profile-logout-button`, `data-page="home"`. **RỦI RO JS:** **CÓ - cao**.
- [ ] **2E.2 Redesign feature/security information** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuyển các feature card thành nhóm thông tin dễ quét về dữ liệu, tiền xử lý và giới hạn y tế; giảm trang trí tối không cần thiết, dùng token dark đúng hệ. **RỦI RO JS:** Không.
- [ ] **2E.3 Redesign FAQ/contact** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Chuẩn hóa FAQ, label thật cho input/textarea, CTA và trạng thái gửi; nếu chưa có logic gửi thì không mô phỏng thành công giả. **RỦI RO JS:** Thấp.
- [ ] **2F Nghiệm thu toàn bộ màn** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Antigravity verify Auth -> Home -> Result -> History -> Profile ở Light/Dark, đối chiếu frame Figma tương ứng và xác nhận mọi luồng hiện có vẫn hoạt động. **RỦI RO JS:** **CÓ - cao**.

## Giai đoạn 3 — Responsive và trạng thái

- [ ] **3.1 Desktop >1024px** — **File:** `frontend/styles.css`. **Mô tả:** Sidebar 240px, content max 1200px, result summary sticky, form/result grids đúng spec; kiểm tra 1280, 1440 và màn rộng không kéo giãn dòng quá dài. **RỦI RO JS:** Không.
- [ ] **3.2 Tablet 640-1024px** — **File:** `frontend/styles.css`. **Mô tả:** Sidebar chuyển icon-rail 72px hoặc ẩn theo phương án thống nhất; các grid xếp lại không che nội dung, bảng summary vẫn đọc được, sticky được tắt khi không phù hợp. **RỦI RO JS:** Thấp nếu không đổi `[data-page]`.
- [ ] **3.3 Mobile <640px** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Dùng bottom nav, chừa safe-area và padding đáy; button/control tối thiểu 44x44px; stack form, result, history, auth; không overflow ngang ở 320px. **RỦI RO JS:** **CÓ - cao** với nav.
- [ ] **3.4 Loading auth/symptom/predict** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Dùng skeleton/progress và disabled state; đặt `aria-busy` cho vùng đang cập nhật; ngăn submit lặp trong khi request chạy; luôn khôi phục control trong `finally`. **RỦI RO JS:** **CÓ - nghiêm trọng**, vì thay luồng async.
- [ ] **3.5 Empty states** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Bao phủ chưa nhập, chưa có kết quả, symptom search rỗng, chưa có lịch sử, top predictions rỗng; mỗi trạng thái có mô tả và hành động tiếp theo. **RỦI RO JS:** **CÓ - cao**.
- [ ] **3.6 Error/offline states** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Phân biệt validation, auth error, API error, mất mạng; giữ nội dung người dùng đã nhập; thêm retry có mục đích và thông báo live-region. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **3.7 Trạng thái không đủ tin cậy** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Không hiển thị như một chẩn đoán chắc chắn; có icon + nhãn chữ + giải thích + CTA bổ sung triệu chứng/đi khám; không gợi ý thuốc khi `<50%` hoặc backend trả `needs_more_input`. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **3.8 Reduced motion và animation restraint** — **File:** `frontend/styles.css`. **Mô tả:** Chỉ dùng transition ngắn cho theme, nav, chip, progress/skeleton; hỗ trợ `prefers-reduced-motion`; không dùng chuyển động gây hiểu sai trạng thái y tế. **RỦI RO JS:** Không.
- [ ] **3.9 Kiểm tra nội dung dài và tiếng Việt** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Test tên bệnh/thuốc dài, danh sách dài, email dài, zoom 200%, wrap hợp lý, không cắt mất thông tin quan trọng; xác nhận encoding UTF-8 hiển thị đúng dấu. **RỦI RO JS:** Thấp.

## Giai đoạn 4 — QA và Antigravity verify

- [ ] **4.1 Kiểm tra DOM contract tự động/thủ công** — **File:** `frontend/index.html`, `frontend/script.js`. **Mô tả:** So sánh lại đủ 53 ID, không ID trùng, đủ `page-home|result|history|about`, đủ `[data-page]`, `[data-auth-view]`, `[data-auth-target]`, class trạng thái và selector động. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **4.2 Smoke test auth** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Verify login, register, forgot, reset, session restore, logout từ sidebar và profile; lỗi/success message đúng, focus được đưa tới vùng hợp lý. **RỦI RO JS:** **CÓ - cao**.
- [ ] **4.3 Smoke test dự đoán** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Verify nhập text, đếm ký tự, tìm/chọn/bỏ chip, ví dụ, xóa trắng, submit, loading, success, insufficient input, error, suggested symptom rerun. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **4.4 Smoke test kết quả và lịch sử** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Verify mọi trường kết quả được cập nhật, gauge/prediction bar đúng dữ liệu, lưu kết quả, card sinh động đồng bộ style, search history và điều hướng trở lại. **RỦI RO JS:** **CÓ - nghiêm trọng**.
- [ ] **4.5 Verify dark mode** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Test toggle trên auth/app, reload, đăng nhập/đăng xuất, system theme lần đầu, localStorage, icon/`aria-pressed`, mọi semantic color ở cả hai mode. **RỦI RO JS:** **CÓ - cao**.
- [ ] **4.6 Audit accessibility** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Contrast >=4.5:1; tab order logic; focus ring; icon-button có `aria-label`; heading/landmark hợp lý; label/description cho form; live-region không spam; trạng thái có text/icon ngoài màu; target mobile >=44px. **RỦI RO JS:** **CÓ - cao** nếu cần điều chỉnh focus/live state.
- [ ] **4.7 Verify responsive** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** Antigravity chụp và kiểm tra tối thiểu 320x568, 390x844, 768x1024, 1024x768, 1280x720, 1440x900 ở Light/Dark; không overflow, che bottom-nav hoặc mất CTA. **RỦI RO JS:** Thấp.
- [ ] **4.8 Đối chiếu Figma** — **File:** `frontend/index.html`, `frontend/styles.css`. **Mô tả:** So sánh token, typography, spacing, component variants và các frame `Auth`, `Home`, `Result`, `History`, `Profile` Desktop/Mobile; ghi sai lệch có chủ đích thay vì hardcode để giống ảnh. **RỦI RO JS:** Không.
- [ ] **4.9 Kiểm tra chất lượng kỹ thuật** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Không lỗi console, không request lỗi ngoài fixture mong đợi, không selector null, không màu/spacing hardcode còn sót khi đã có token, không CSS chết rõ ràng, không thêm dependency. **RỦI RO JS:** **CÓ - cao**.
- [ ] **4.10 Regression cuối trước merge** — **File:** `frontend/index.html`, `frontend/styles.css`, `frontend/script.js`. **Mô tả:** Chạy lại toàn bộ luồng trên browser bằng Antigravity; chỉ merge khi contract DOM, auth, dự đoán, lưu lịch sử, điều hướng, theme, responsive và a11y đều đạt. **RỦI RO JS:** **CÓ - nghiêm trọng**.

## Điều kiện hoàn thành

- [ ] Toàn bộ UI Auth, Trang chủ, Kết quả, Lịch sử, Hồ sơ dùng chung token/component mới và hoạt động ở Light/Dark.
- [ ] Trang Kết quả thể hiện rõ trong 3 giây: kết luận dự kiến, mức tin cậy, nhóm thuốc và cảnh báo an toàn.
- [ ] Không đổi/xóa bất kỳ ID hoặc selector/data contract mà `frontend/script.js` đang dùng.
- [ ] Không thêm framework, bundler hoặc dependency frontend.
- [ ] Mọi trạng thái quan trọng có chữ/icon ngoài màu; contrast, focus ring và touch target đạt yêu cầu.
- [ ] Antigravity đã verify từng trang, từng breakpoint, Light/Dark và các luồng regression trước khi merge.
